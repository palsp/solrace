import React from "react";
import styled from "styled-components";
const Roadmap = () => {
  return <WrapperRoadmap>Roadmap</WrapperRoadmap>;
};

const WrapperRoadmap = styled.div`
  width: 100vw;
  height: 100vh;
  background: var(--background-gradient-1);
`;
export default Roadmap;
