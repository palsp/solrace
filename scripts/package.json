{
  "scripts": {
    "copy" : "mkdir -p ./metaplex/assets && cp -r ./hashlips_art_engine/build/images/* ./metaplex/assets && cp -r ./hashlips_art_engine/build/json/* ./metaplex/assets && rm -rf ./metaplex/assets/_metadata.json", 
    "copy:backend" : "rm -rf ../backend/assets/ && mkdir -p ../backend/assets && cp -r ./hashlips_art_engine/build/json/* ../backend/assets",
    "clean" : "rm -rf ./metaplex/assets"
  }
}