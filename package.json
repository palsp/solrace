{
    "name": "solrace",

    "scripts": {
        "test": "anchor test --skip-local-validator --skip-build --skip-deploy --skip-lint",
        "test:redeploy": "anchor test --skip-local-validator  --skip-lint",
        "build": "anchor build -p sol_race_core --skip-lint && anchor run copy_types_frontend && anchor run copy_types_backend && anchor run copy_types_cli",
        "deploy": "anchor deploy -p sol_race_core"
    },
    "dependencies": {
        "@project-serum/anchor": "^0.22.1"
    },
    "devDependencies": {
        "@project-serum/common": "^0.0.1-beta.3",
        "@types/chai": "^4.3.0",
        "@types/faker": "5.5.1",
        "@types/mocha": "^9.0.0",
        "chai": "^4.3.6",
        "chai-as-promised": "^7.1.1",
        "chai-bn": "^0.3.1",
        "faker": "5.5.3",
        "mocha": "^9.0.3",
        "ts-mocha": "^8.0.0",
        "typescript": "^4.3.5"
    }
}
